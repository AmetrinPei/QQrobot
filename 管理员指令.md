# 管理员常用指令

仅 **管理员 QQ**（`.env.dev` 里的 `ADMIN_QQ`）在**私聊机器人**时可用。  
群聊小框不要用来改记忆。本地底稿在 `persona.txt`；**管理员可用 QQ 指令覆盖人设/形象，且优先生效**，无需改文件重启。

---

## 使用前提

1. 在 `.env.dev` 配置好管理员 QQ，例如：

```env
ADMIN_QQ=3107482871
```

多个管理员用英文逗号分隔：`ADMIN_QQ=111,222`

2. **私聊**机器人发送下列指令（群聊发无效 / 会被当普通聊天）。
3. 修改 `.env.dev` 后需重启；人设/形象用下面指令可即时生效。

---

## 启停机器人

| 指令 | 说明 |
|------|------|
| `/启动` 或 `/开` | 恢复正常回复 |
| `/停止` 或 `/关` | 暂停回复（群聊/私聊闲聊都不回；主动插话也停） |
| `/状态` | 查看运行状态、是否有管理员人设/形象覆盖 |

说明：停止后**管理员私聊指令仍可用**（含 `/启动`）。这是软开关，进程不必退出。

---

## 人设与形象（管理员优先）

| 指令 | 说明 | 示例 |
|------|------|------|
| `/人设` | 查看当前管理员人设修订 | `/人设` |
| `/人设 内容` | 覆盖写入人设修订（优先于 persona.txt） | `/人设 说话更短，更温柔` |
| `/人设追加 内容` | 在现有修订后追加 | `/人设追加 更爱聊漫画` |
| `/人设清空` | 清空人设修订 | `/人设清空` |
| `/形象` | 查看当前形象设定 | `/形象` |
| `/形象 描述` | 覆盖写入形象（外貌/头像/气质等） | `/形象 头像是Q版芙莉莲，气质温润` |
| `/形象清空` | 清空形象设定 | `/形象清空` |

说明：管理员写入的人设/形象会注入提示词并标明**最高优先**；与本地 `persona.txt` 冲突时以管理员为准。

---

## 代发（主动说话，内容由你定）

仅管理员私聊可用；**原文照发**，不经过模型改写。

| 指令 | 说明 | 示例 |
|------|------|------|
| `/说群 群号 内容` | 让机器人在指定群发言 | `/说群 123456 大家好呀` |
| `/说私 QQ号 内容` | 让机器人私聊某人 | `/说私 123456789 在吗` |
| `/说 群 群号 内容` | 同上 | `/说 群 123456 大家好呀` |
| `/说 私 QQ号 内容` | 同上 | `/说 私 123456789 在吗` |

说明：

- 机器人必须已在该群，或已与该 QQ 有好友/可私聊关系，否则会发送失败。
- 发出的内容会写入对应会话的短期记忆，后续对话能接上。

---

## 通讯录（认人）与关系

| 指令 | 说明 | 示例 |
|------|------|------|
| `/备注 QQ号 称呼` | 给某人起称呼，之后说话人优先显示备注 | `/备注 123456789 小明` |
| `/备注 清除 QQ号` | 取消该 QQ 的备注 | `/备注 清除 123456789` |
| `/记忆列表` | 查看已备注的人名与亲疏 | `/记忆列表` |
| `/关系 QQ号 等级` | 设定亲疏：陌生人 / 熟人 / 朋友 / 家人 | `/关系 3107482871 家人` |
| `/关系 QQ号` | 查看某人当前关系与互动次数 | `/关系 123456789` |

说明：

- 谁和机器人说过话会自动记入通讯录；**只有手动备注**才会出现在 `/记忆列表`。
- 备注后模型看到的说话人类似：`说话人：小明(QQ:123456789，熟人)`。
- 互动多了会自动升到「熟人」，**朋友/家人需手动设定**（建议把哥哥设为家人）。
- 亲疏会影响接话语气；私聊主动关心也只对朋友/家人（可配置）。

---

## 心情状态

| 指令 | 说明 | 示例 |
|------|------|------|
| `/心情` | 查看当前心情与精力 | `/心情` |
| `/心情 平静\|开心\|害羞\|委屈\|烦躁 [精力]` | 手动设定 | `/心情 开心 80` |
| `/心情 重置` | 重置为平静、精力 70 | `/心情 重置` |

说明：心情会随对话与昼夜轻微变化，并注入人设上下文；闲聊仍简短，认真求助时按逻辑分段。

---

## 短期记忆（聊两句别忘刚才说啥）

| 指令 | 说明 | 示例 |
|------|------|------|
| `/忘记短期` | 清空**你和机器人私聊**的近期对话窗口 | `/忘记短期` |
| `/忘记短期 群号` | 清空指定群的近期对话窗口 | `/忘记短期 123456` |
| `/忘记短期 群 群号` | 同上 | `/忘记短期 群 123456` |

说明：

- 默认保留最近约 **16** 轮（`.env.dev` 的 `MEMORY_SHORT_TERM_TURNS`）。
- 私聊按「用户」存，群聊按「群」存；不会无限堆历史。
- 觉得机器人串戏、记错刚才内容时，先 `/忘记短期`。

---

## 长期记忆（隔天还想记得的事）

| 指令 | 说明 | 示例 |
|------|------|------|
| `/记住 内容` | **管理员优先**长期记忆（冲突时以管理员为准） | `/记住 塔米米头像是芙莉莲` |
| `/忘掉 关键词` | 删除内容里包含该关键词的记忆 | `/忘掉 喜欢猫` |
| `/长期记忆` | 查看最近若干条（含 `[admin]` 优先条目） | `/长期记忆` |
| `/长期记忆 清空` | 清空全部长期记忆 | `/长期记忆 清空` |

说明：

- `/记住` 来源为 `admin`，检索时置顶并优先采信；自动抽取为 `auto`，优先级更低。
- 群聊是否自动记长期事实由 `MEMORY_LONG_TERM_GROUP` 控制。
- 不要记密码、验证码、隐私八卦；不确定时用 `/忘掉` 清掉。

---

## 日常聊天（非指令）

这些不是管理指令，任何人都能用：

| 场景 | 怎么触发 |
|------|----------|
| 私聊 | 直接发文字（或带图） |
| 群聊 | **@机器人** 后说话/发图；**@ 后短时间内**的跟进文字/表情也会分析，必要时才回；也可**回复**机器人消息。多人同时聊时回复会带 @。另：约每 10 分钟若有新消息，可能主动轻轻接一句（插不上就沉默） |
| 识图 | 默认关闭（`VISION_ENABLED=false`）：含图片的消息不回应；设为 `true` 后才会识图再答 |
| 拟人细节 | 会带上当前时间与心情；必要时用 `---` 拆成多段短消息发送 |

人设与说话风格：底稿编辑 `persona.txt`；日常微调优先用私聊 `/人设` `/形象`（管理员优先、即时生效）。

---

## 速查（复制用）

```text
/说群 123456 大家好呀
/说私 123456789 在吗

/备注 123456789 小明
/备注 清除 123456789
/记忆列表
/关系 3107482871 家人
/关系 123456789
/心情
/心情 重置

/忘记短期
/忘记短期 群号

/启动
/停止
/状态

/人设 说话更短更温柔
/人设追加 更爱聊漫画
/形象 头像是Q版芙莉莲
/人设
/形象

/记住 要长期记住的内容
/忘掉 关键词
/长期记忆
/长期记忆 清空
```

---

## 相关配置（`.env.dev`）

| 配置项 | 含义 |
|--------|------|
| `ADMIN_QQ` | 可执行上述指令的 QQ |
| `MEMORY_SHORT_TERM_TURNS` | 短期记忆保留轮数 |
| `MEMORY_LONG_TERM_ENABLED` | 是否启用长期记忆 |
| `MEMORY_LONG_TERM_GROUP` | 群聊是否自动抽长期事实 |
| `GROUP_FOLLOWUP_SECONDS` | @/互动后跟进窗口（秒，文字+表情，默认 180） |
| `GROUP_AT_WHEN_ACTIVE` | 同时互动人数达到该值时回复带 @（默认 2） |
| `GROUP_PROACTIVE_ENABLED` | 是否开启定时主动插话 |
| `GROUP_PROACTIVE_INTERVAL_SECONDS` | 主动插话检查间隔（秒，默认 600=10 分钟） |
| `GROUP_PROACTIVE_MIN_MESSAGES` | 至少几条新消息才考虑主动开口 |
| `PRIVATE_PROACTIVE_ENABLED` | 是否对高关系用户低频私聊关心（默认 false） |
| `PRIVATE_PROACTIVE_IDLE_SECONDS` | 多久未私聊才考虑关心（默认 86400=1 天） |
| `PRIVATE_PROACTIVE_COOLDOWN_SECONDS` | 同一人关心冷却（默认 172800=2 天） |
| `PRIVATE_PROACTIVE_MIN_TIER` | 最低亲疏（默认 朋友） |
| `PRIVATE_PROACTIVE_WHITELIST` | 可选 QQ 白名单，逗号分隔 |
| `SILICONFLOW_BASE_URL` | API 地址（DeepSeek 官方：`https://api.deepseek.com`） |
| `SILICONFLOW_MODEL` | 文本对话模型（DeepSeek：`deepseek-v4-flash`） |
| `VISION_ENABLED` | 是否开启识图（默认 `false`；关闭时含图消息不回应） |
| `SILICONFLOW_VISION_MODEL` | 识图用的视觉模型（仅 `VISION_ENABLED=true` 时生效；DeepSeek 官方暂无识图可留空） |

数据文件：`data/memory.db`（短期会话、通讯录、长期事实、心情状态）。
