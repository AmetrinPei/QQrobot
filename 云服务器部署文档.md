# QQbot 云服务器部署操作文档

本文档面向：**已租好一台 Linux 云服务器**，要把本项目（NoneBot2 + NapCat + DeepSeek/SiliconFlow）完整跑起来。

默认假设：

- 系统：**Ubuntu 22.04**（其他 Debian 系可类推）
- NapCat 与机器人在**同一台服务器**
- 只部署主机器人 `bot.py`（皮皮塔米米）；`bot_niko.py` 见文末附录
- 连接方式：NoneBot **正向 WebSocket** 连接 NapCat 的 `3001` 端口（与本地 `.env.dev` 一致）

架构：

```
QQ ←→ NapCat(:3001) ←ws→ NoneBot bot.py(:8080)
              ↓
     DeepSeek / SiliconFlow API
```

---

## 目录

0. [部署前准备](#0-部署前准备)
1. [登录云服务器](#1-登录云服务器)
2. [安装 Docker](#2-安装-docker)
3. [部署 NapCat（QQ 协议端）](#3-部署-napcatqq-协议端)
4. [登录机器人 QQ](#4-登录机器人-qq)
5. [配置正向 WebSocket](#5-配置正向-websocket)
6. [上传并安装 QQbot](#6-上传并安装-qqbot)
7. [配置环境变量](#7-配置环境变量)
8. [用 systemd 常驻运行](#8-用-systemd-常驻运行)
9. [验收测试](#9-验收测试)
10. [日常运维](#10-日常运维)
11. [常见问题](#11-常见问题)
12. [附录](#12-附录)

---

## 0. 部署前准备

### 0.1 你需要提前有的东西

| 项目 | 说明 |
|------|------|
| 云服务器公网 IP | 例如 `1.2.3.4` |
| SSH 登录账号 | 常见为 `ubuntu` / `root` |
| SSH 密钥或密码 | 能登录即可 |
| 机器人 QQ 号 | 用来扫码登录 NapCat |
| DeepSeek / SiliconFlow API Key | 与本地 `.env.dev` 里一致 |
| 本机项目目录 | 例如 `D:\Qoder\QQbot` |

### 0.2 云厂商安全组（防火墙）建议

| 端口 | 是否对外开放 | 原因 |
|------|--------------|------|
| `22`（SSH） | ✅ 建议开（可限制你的家宽 IP） | 远程管理 |
| `3001`（NapCat WS） | ❌ 不要开 | 本机回环通信即可 |
| `6099`（NapCat WebUI） | ❌ 不要开 | 用 SSH 隧道访问更安全 |
| `8080`（NoneBot） | ❌ 不要开 | 同样只给本机用 |

### 0.3 本机需要准备的工具

- Windows：PowerShell 即可（系统自带）
- 建议安装 [Git](https://git-scm.com/)（若用 Git 拉代码）
- 可选：WinSCP / 宝塔面板（图形化传文件更轻松）

### 0.4 建议先在本地确认能跑通

本地若已能正常收发消息，再迁移到云，可减少一次排错面。

---

## 1. 登录云服务器

### 1.1 Windows 用 PowerShell 登录

把下面的 `ubuntu` 和 `1.2.3.4` 换成你的账号和 IP：

```powershell
ssh ubuntu@1.2.3.4
```

第一次连接会问：

```text
Are you sure you want to continue connecting (yes/no)?
```

输入 `yes` 回车。随后输入密码（输入时屏幕不显示字符，属正常）。

### 1.2 确认系统

登录成功后执行：

```bash
uname -a
cat /etc/os-release
whoami
pwd
```

看到类似 `Ubuntu`、当前用户名、`/home/ubuntu` 即可。

> 若你是 `root` 登录，下文路径请把 `/home/ubuntu` 改成 `/root`，用户名改成 `root`。

---

## 2. 安装 Docker

### 2.1 更新软件源并安装

```bash
sudo apt update
sudo apt install -y docker.io docker-compose-v2 curl
```

### 2.2 启动 Docker 并设置开机自启

```bash
sudo systemctl enable --now docker
sudo systemctl status docker
```

状态里应看到 `active (running)`。按 `q` 退出状态页。

### 2.3 让当前用户可直接用 docker（免 sudo）

```bash
sudo usermod -aG docker $USER
```

**必须重新登录一次 SSH**，组权限才会生效：

```bash
exit
```

再重新：

```powershell
ssh ubuntu@1.2.3.4
```

验证：

```bash
docker version
docker ps
```

能输出版本、且 `docker ps` 不报权限错误，即成功。

---

## 3. 部署 NapCat（QQ 协议端）

### 3.1 创建数据目录

用于持久化登录态与配置，容器删了也能保留：

```bash
mkdir -p ~/napcat/config ~/napcat/ntqq
cd ~/napcat
pwd
# 应显示：/home/ubuntu/napcat
```

### 3.2 拉取镜像并启动容器

```bash
docker pull mlikiowa/napcat-docker:latest

docker run -d \
  --name napcat \
  --restart=always \
  -e NAPCAT_UID=$(id -u) \
  -e NAPCAT_GID=$(id -g) \
  -p 127.0.0.1:3001:3001 \
  -p 127.0.0.1:6099:6099 \
  -v ~/napcat/config:/app/napcat/config \
  -v ~/napcat/ntqq:/app/.config/QQ \
  mlikiowa/napcat-docker:latest
```

参数说明：

| 参数 | 作用 |
|------|------|
| `--name napcat` | 容器名，方便以后 `docker logs napcat` |
| `--restart=always` | 服务器重启后自动拉起 |
| `NAPCAT_UID/GID` | 文件权限与当前用户一致 |
| `127.0.0.1:3001:3001` | 正向 WS，仅本机可连 |
| `127.0.0.1:6099:6099` | WebUI，仅本机可访问 |
| `~/napcat/config` | NapCat 配置持久化 |
| `~/napcat/ntqq` | QQ 登录数据持久化 |

### 3.3 确认容器在跑

```bash
docker ps
```

应看到名为 `napcat` 的容器，`STATUS` 为 `Up ...`。

若看不到，查失败原因：

```bash
docker ps -a
docker logs napcat
```

---

## 4. 登录机器人 QQ

### 4.1 查看 WebUI Token

```bash
docker logs napcat
```

在输出中找到类似：

```text
WebUi Local Panel Url: http://127.0.0.1:6099/webui?token=xxxxxxxx
```

把 `token=xxxxxxxx` 整段抄下来备用。

> 日志很长时可用：`docker logs napcat 2>&1 | grep -i token`

### 4.2 用 SSH 隧道在本机打开 WebUI

**不要把 6099 直接对公网开放。**  
在你自己的 Windows PowerShell（新开一个窗口，保持登录会话）执行：

```powershell
ssh -L 6099:127.0.0.1:6099 ubuntu@1.2.3.4
```

这条命令成功登录后**不要关掉这个窗口**（关掉隧道就断了）。

### 4.3 浏览器访问

在你自己电脑浏览器打开：

```text
http://127.0.0.1:6099/webui?token=你刚才抄的token
```

### 4.4 扫码登录

1. 进入 QQ 登录页
2. 选择二维码登录
3. 用**机器人 QQ** 扫码并确认
4. 等待提示登录成功

若提示风险验证，按 QQ 提示完成验证后再试。

### 4.5 确认登录态会保留

登录成功后，数据在 `~/napcat/ntqq`。以后即使：

```bash
docker restart napcat
```

一般也不需要重新扫码（除非 QQ 主动下线或数据目录被删）。

---

## 5. 配置正向 WebSocket

目标：让 NapCat 在 `3001` 端口提供 **WebSocket 服务端**，供 NoneBot 主动连接。

### 5.1 打开网络配置

在 NapCat WebUI：

1. 进入 **网络配置**
2. 点击 **新建**
3. 选择 **WebSocket 服务端**（也叫正向 WS）

### 5.2 按下列参数填写

| 配置项 | 建议值 | 说明 |
|--------|--------|------|
| 名称 | `ws-server` | 任意，不与其它重复即可 |
| 启用 | ✅ 勾选 | 必须启用 |
| host | `0.0.0.0` | 容器内监听全部网卡 |
| port | `3001` | 必须与机器人配置一致 |
| 消息格式 | `array` | 推荐 array |
| token | 留空 | 与当前 `.env.dev` 默认一致 |
| 心跳间隔 | `30000` | 默认即可 |

### 5.3 保存并生效

点击保存。若界面提示需要重启：

```bash
docker restart napcat
```

### 5.4 确认 3001 在监听

```bash
ss -lntp | grep 3001
```

应能看到 `127.0.0.1:3001`（或 `*:3001` 经 Docker 映射后本机可访问）。

> 若你给 WS 设置了 token，后面配置机器人时必须同步填写 `ONEBOT_ACCESS_TOKEN`。

---

## 6. 上传并安装 QQbot

下面提供两种方式，任选其一。

### 方式 A：用 scp 从本机上传（适合未推 Git 仓库）

在 **你自己电脑** 的 PowerShell 中，进入项目上级目录后执行：

```powershell
cd D:\Qoder
scp -r QQbot ubuntu@1.2.3.4:~/QQbot
```

注意：

- 不要指望把本机 `.venv` 拷过去还能用（Windows venv 在 Linux 不可用）
- `.env` / `.env.dev` 若被忽略，可能没传上去，第 7 步会单独处理

上传后回到服务器检查：

```bash
ls ~/QQbot
# 应能看到 bot.py、pyproject.toml、src、persona.txt 等
```

### 方式 B：服务器上 git clone（适合代码已在 Git 托管）

```bash
cd ~
git clone <你的仓库地址> QQbot
cd QQbot
```

### 6.1 安装 Python 与虚拟环境

```bash
sudo apt install -y python3 python3-venv python3-pip
cd ~/QQbot
python3 -m venv .venv
source .venv/bin/activate
python -V
pip install -U pip
pip install -e .
```

安装成功后，`pip list` 中应能看到 `nonebot2`、`nonebot-adapter-onebot` 等。

### 6.2 准备数据目录（可选：迁移旧记忆）

```bash
mkdir -p ~/QQbot/data
```

若希望保留本地已有记忆，在**本机 PowerShell**上传数据库：

```powershell
scp D:\Qoder\QQbot\data\memory.db ubuntu@1.2.3.4:~/QQbot/data/memory.db
```

人设文件确认存在：

```bash
ls ~/QQbot/persona.txt
```

---

## 7. 配置环境变量

NoneBot 默认会按 `ENVIRONMENT` 加载对应环境文件。本项目本地常用 `.env.dev`。

### 7.1 创建 / 编辑 `.env.dev`

```bash
cd ~/QQbot
nano .env.dev
```

最小可用示例（**请替换 API Key 与管理员 QQ**）：

```env
HOST=127.0.0.1
PORT=8080
DRIVER=~fastapi+~httpx+~websockets

# 正向 WebSocket：NoneBot 主动连接 NapCat
ONEBOT_WS_URLS=["ws://127.0.0.1:3001"]

# 若 NapCat 里配置了 token，取消下一行注释并填写相同值
# ONEBOT_ACCESS_TOKEN=

# ========== API ==========
SILICONFLOW_API_KEY=你的密钥
SILICONFLOW_BASE_URL=https://api.deepseek.com
SILICONFLOW_MODEL=deepseek-v4-flash
VISION_ENABLED=false
SILICONFLOW_VISION_MODEL=

# ========== 记忆 / 管理 ==========
ADMIN_QQ=你的QQ号
MEMORY_SHORT_TERM_TURNS=16
MEMORY_LONG_TERM_ENABLED=true
MEMORY_LONG_TERM_GROUP=true
MEMORY_LONG_TERM_RETRIEVE=8
MEMORY_LONG_TERM_MAX=200
GROUP_FOLLOWUP_SECONDS=60
GROUP_AT_WHEN_ACTIVE=3
GROUP_PROACTIVE_ENABLED=true
GROUP_PROACTIVE_INTERVAL_SECONDS=600
GROUP_PROACTIVE_MIN_MESSAGES=2
GROUP_PROACTIVE_COOLDOWN_SECONDS=120
GROUP_PROACTIVE_MAX_CONTEXT=20

PRIVATE_PROACTIVE_ENABLED=false
PRIVATE_PROACTIVE_INTERVAL_SECONDS=3600
PRIVATE_PROACTIVE_IDLE_SECONDS=86400
PRIVATE_PROACTIVE_COOLDOWN_SECONDS=172800
PRIVATE_PROACTIVE_MIN_TIER=朋友
PRIVATE_PROACTIVE_WHITELIST=
```

同时创建环境标识文件（若还没有）：

```bash
nano ~/QQbot/.env
```

写入一行：

```env
ENVIRONMENT=dev
```

保存方法（nano）：

1. `Ctrl + O` 回车保存
2. `Ctrl + X` 退出

### 7.2 从本机直接上传配置（更省事）

若本机已有可用的 `.env.dev`，在本机 PowerShell：

```powershell
scp D:\Qoder\QQbot\.env.dev ubuntu@1.2.3.4:~/QQbot/.env.dev
scp D:\Qoder\QQbot\.env ubuntu@1.2.3.4:~/QQbot/.env
```

> 切勿把含密钥的文件提交到公开 Git 仓库。

### 7.3 权限建议

```bash
chmod 600 ~/QQbot/.env ~/QQbot/.env.dev
```

---

## 8. 用 systemd 常驻运行

先手动试跑，确认没问题再做成服务。

### 8.1 手动试跑

确认 NapCat 已登录且 3001 正常后：

```bash
cd ~/QQbot
source .venv/bin/activate
python bot.py
```

期望现象：

- 终端有 NoneBot 启动日志
- 出现 OneBot / WebSocket 连接成功相关信息
- 本机监听 `8080`

另开一个 SSH 窗口检查：

```bash
ss -lntp | grep 8080
```

用手机 QQ 给机器人发一句私聊，看终端是否收到事件并回复。  
测试成功后，在运行窗口按 `Ctrl + C` 停掉。

### 8.2 创建 systemd 服务

```bash
sudo nano /etc/systemd/system/qqbot.service
```

写入（**把 `ubuntu` 换成你的实际用户名**）：

```ini
[Unit]
Description=QQbot NoneBot
After=network-online.target docker.service
Wants=network-online.target docker.service

[Service]
Type=simple
User=ubuntu
Group=ubuntu
WorkingDirectory=/home/ubuntu/QQbot
Environment=ENVIRONMENT=dev
ExecStart=/home/ubuntu/QQbot/.venv/bin/python bot.py
Restart=always
RestartSec=5
# 日志进 journald，也便于排查
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

保存退出。

### 8.3 启用并启动

```bash
sudo systemctl daemon-reload
sudo systemctl enable qqbot
sudo systemctl start qqbot
sudo systemctl status qqbot
```

应看到 `active (running)`。

看实时日志：

```bash
sudo journalctl -u qqbot -f
```

按 `Ctrl + C` 可退出日志跟随（不会停机器人）。

---

## 9. 验收测试

按顺序勾：

1. `docker ps` 中 `napcat` 为 Up  
2. `systemctl status qqbot` 为 active  
3. `ss -lntp | grep -E '3001|8080'` 都能看到监听  
4. QQ 私聊机器人，能正常回复  
5. 群里 @ 机器人，能正常回复（机器人需已在群内）  
6. 管理员私聊发 `/心情`，能返回状态（见 `管理员指令.md`）  
7. 重启验证：

```bash
sudo reboot
```

重启后重新 SSH 登录，再检查：

```bash
docker ps
sudo systemctl status qqbot
```

两者都应自动恢复。

---

## 10. 日常运维

### 10.1 常用命令

```bash
# —— NapCat ——
docker logs -f napcat          # 看协议端日志
docker restart napcat          # 重启协议端
docker stop napcat
docker start napcat

# —— 机器人 ——
sudo systemctl status qqbot
sudo systemctl restart qqbot
sudo systemctl stop qqbot
sudo journalctl -u qqbot -n 100 --no-pager
sudo journalctl -u qqbot -f
```

### 10.2 更新机器人代码

```bash
cd ~/QQbot
# 若用 git：
# git pull

source .venv/bin/activate
pip install -e .
sudo systemctl restart qqbot
```

### 10.3 更新 NapCat 镜像

```bash
docker pull mlikiowa/napcat-docker:latest
docker rm -f napcat

docker run -d \
  --name napcat \
  --restart=always \
  -e NAPCAT_UID=$(id -u) \
  -e NAPCAT_GID=$(id -g) \
  -p 127.0.0.1:3001:3001 \
  -p 127.0.0.1:6099:6099 \
  -v ~/napcat/config:/app/napcat/config \
  -v ~/napcat/ntqq:/app/.config/QQ \
  mlikiowa/napcat-docker:latest
```

因为挂载了 `~/napcat/...`，一般不用重新扫码；但仍需确认 WebUI 里正向 WS 仍启用。

### 10.4 修改人设 / 配置后

```bash
# 改完 persona.txt 或 .env.dev 后：
sudo systemctl restart qqbot
```

### 10.5 备份（建议定期做）

```bash
# 备份记忆与人设
tar -czf ~/qqbot-backup-$(date +%F).tar.gz \
  -C ~ QQbot/data QQbot/persona.txt QQbot/.env.dev

# 备份 NapCat 登录与配置
tar -czf ~/napcat-backup-$(date +%F).tar.gz -C ~ napcat
```

把生成的 `.tar.gz` 下载到本地保存。

---

## 11. 常见问题

### 11.1 `docker ps` 报 permission denied

未重新登录 SSH，或未加入 docker 组：

```bash
sudo usermod -aG docker $USER
exit
# 重新 ssh 登录后再试
```

### 11.2 WebUI 打不开

1. 确认隧道命令仍在运行：`ssh -L 6099:127.0.0.1:6099 ...`
2. 确认容器在跑：`docker ps`
3. 确认 token：`docker logs napcat | grep -i token`
4. 浏览器地址必须是本机 `127.0.0.1:6099`，不是服务器公网 IP（除非你刻意开放了端口）

### 11.3 机器人启动了但不回消息

逐项检查：

```bash
# NapCat 是否在线
docker logs napcat --tail 50

# NoneBot 是否连上 WS
sudo journalctl -u qqbot -n 80 --no-pager

# 端口
ss -lntp | grep -E '3001|8080'
```

常见原因：

- NapCat 未登录 / 掉线
- WebUI 里正向 WS 未启用或端口不是 `3001`
- `.env.dev` 里 `ONEBOT_WS_URLS` 写错
- 两边 token 不一致
- API Key 无效或欠费（表现为收到消息但不回复 / 报错）

### 11.4 403 / 鉴权失败

NapCat 的 WS token 与机器人 `ONEBOT_ACCESS_TOKEN` 必须完全一致；一边有一边没有也会失败。

### 11.5 QQ 频繁掉线 / 风控

- 尽量固定一台服务器长期运行
- 不要多地同时登录同一 QQ
- 降低主动消息频率（可先关 `GROUP_PROACTIVE_ENABLED`）
- 按 QQ 提示完成安全验证

### 11.6 8080 端口被占用

```bash
ss -lntp | grep 8080
sudo systemctl stop qqbot
# 确认没有残留 python bot.py 后再启动
```

### 11.7 想看 NapCat 二维码但日志刷屏

```bash
docker logs napcat 2>&1 | tail -n 100
```

或重新打开 WebUI 使用二维码登录页。

---

## 12. 附录

### 12.1 一键检查脚本（可选）

```bash
nano ~/check-qqbot.sh
```

内容：

```bash
#!/usr/bin/env bash
set -e
echo "=== docker napcat ==="
docker ps --filter name=napcat --format 'table {{.Names}}\t{{.Status}}\t{{.Ports}}'
echo
echo "=== systemd qqbot ==="
systemctl is-active qqbot || true
echo
echo "=== ports ==="
ss -lntp | grep -E ':3001|:8080|:6099' || true
echo
echo "=== recent qqbot logs ==="
sudo journalctl -u qqbot -n 20 --no-pager || true
```

```bash
chmod +x ~/check-qqbot.sh
~/check-qqbot.sh
```

### 12.2 同时部署 `bot_niko.py`（尼古喵喵）

注意：

- **不要**与 `bot.py` 抢同一个 `8080`
- 需要**另一个 QQ + 另一套 NapCat**（或改端口并做复杂分流，不推荐新手）

更简单的做法：第二台小号 / 第二套目录，复制本流程，改端口与人设即可。

`bot_niko.py` 会自动使用：

- 人设：`persona_niko.txt`
- 数据：`data/niko/`

### 12.3 官方参考链接

- NapCat 文档：https://napneko.github.io/
- NapCat Docker：https://github.com/NapNeko/NapCat-Docker
- 镜像：https://hub.docker.com/r/mlikiowa/napcat-docker

### 12.4 推荐落地顺序（最短路径）

1. SSH 登录服务器  
2. 安装 Docker  
3. 启动 NapCat → SSH 隧道 → 扫码登录  
4. WebUI 开正向 WS `:3001`  
5. 上传 QQbot → 建 venv → 配 `.env.dev`  
6. 手动 `python bot.py` 验证收发  
7. 写成 `systemd` 开机自启  
8. 做一次 `reboot` 验收  

---

文档版本：与当前仓库 `bot.py` + `.env.dev`（正向 WS `3001`）匹配。  
若你后续改了端口或改用反向 WebSocket，请同步改第 5、7 节。
`)